packet_version: 1
status: reviewed_pilot
created_at: 2026-06-02

target:
  surface: governed_table_entry
  table: port_status_bit_matrix
  entry_id: wPortStatus.bit0.PORT_CONNECTION

evidence:
  spec: usb20
  section: "11.24.2.7.1"
  page: null
  document_ref:
    primary_source_id: usb20_spec
    fallback_source_id: archive_usb20_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    USB 2.0 Section 11.24.2.7.1 is the port-status field surface used for this
    packet. For this pilot, the intended narrow claim is only that
    `wPortStatus.bit0.PORT_CONNECTION` names the port-connection status bit at
    bit position 0. This packet does not claim reset, debounce, timing, or any
    host-side interpretation behavior.
  reviewer_note: >
    Reviewed against the cited USB 2.0 section for the narrow claim that
    `wPortStatus.bit0.PORT_CONNECTION` names the port-connection status bit at
    bit position 0. This review does not extend to timing, debounce, reset,
    state transitions, `ClearPortFeature`, or host-side interpretation.

review_checklist:
  - Confirm Section 11.24.2.7.1 is the correct USB 2.0 surface for port status fields.
  - Confirm the packet stays limited to `PORT_CONNECTION` bit name and bit position.
  - Confirm no timing, state-transition, or `ClearPortFeature` semantics are claimed.
  - Confirm any live promotion remains limited to one governed table entry only.

verification_scope:
  claim: bit_name_and_position_only
  excludes:
    - timing behavior
    - state transition behavior
    - ClearPortFeature behavior
    - full USB compliance

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for a single-entry Phase 8E promotion only. The allowed verified
    scope remains limited to bit name and bit position for
    `wPortStatus.bit0.PORT_CONNECTION`. No page-level or table-level verified
    promotion is authorized by this packet.

non_claims:
  - This packet does not verify downstream port behavior.
  - This packet does not verify host-stack interpretation.
  - This packet does not establish page-level or table-level verification.
