packet_version: 1
status: reviewed_pilot
created_at: 2026-06-04

target:
  surface: governed_table_entry
  table: class_request_matrix
  entry_id: usb20_get_status_hub

evidence:
  spec: usb20
  section: "11.24.2"
  page: null
  document_ref:
    primary_source_id: usb20_spec
    fallback_source_id: archive_usb20_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    GET_STATUS with hub recipient setup fields (bmRequestType 0xA0,
    bRequest GET_STATUS, wIndex=0, wLength=4) are documented at the class
    request linkage layer. This packet verifies only setup field identity.
  reviewer_note: >
    Reviewed for request linkage only. This does not claim runtime semantics,
    host poll timing, or state-machine behavior.

review_checklist:
  - Confirm section linkage and setup fields for usb20_get_status_hub in section 11.24.2.
  - Confirm packet scope is limited to request-linkage identity.
  - Confirm no firmware behavior or host-stack interpretation is claimed.
  - Confirm no full compliance interpretation is attached to this packet.

verification_scope:
  claim: request_linkage_only
  excludes:
    - timing behavior
    - state transition behavior
    - firmware behavior
    - host-stack interpretation
    - full USB compliance

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for single entry-level verified promotion for `usb20_get_status_hub`.
    This promotion is limited to request setup linkage only.

non_claims:
  - This packet does not claim full request sequencing or timing correctness.
  - This packet does not claim project-specific GET_STATUS handling behavior.
