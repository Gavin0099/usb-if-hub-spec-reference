packet_version: 1
status: reviewed_pilot
created_at: 2026-06-06

target:
  surface: governed_table_entry
  table: standard_device_request_matrix
  entry_id: usb20_std_get_status_interface

evidence:
  spec: usb20
  section: "9.4.5"
  page: null
  document_ref:
    primary_source_id: usb20_spec
    fallback_source_id: archive_usb20_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    GET_STATUS with interface recipient: bmRequestType=0x81, bRequest=0x00,
    wValue=0x0000, wIndex=interface_number, wLength=2. Returns 0x0000
    (reserved, both bytes zero). Documented at USB 2.0 §9.4.5.
  reviewer_note: >
    Reviewed for setup field identity and recipient encoding only.
    Does not claim interface state or alternate-setting behavior.

review_checklist:
  - Confirm section linkage and setup fields for usb20_std_get_status_interface in §9.4.5.
  - Confirm packet scope is limited to request-linkage identity.
  - Confirm no firmware behavior or host-stack interpretation is claimed.
  - Confirm no full compliance interpretation is attached to this packet.

verification_scope:
  claim: request_linkage_only
  excludes:
    - timing behavior
    - state transition behavior
    - firmware behavior
    - host-stack interpretation
    - full USB compliance

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for entry-level verified promotion for `usb20_std_get_status_interface`.
    Promotion limited to request setup field linkage only.

non_claims:
  - This packet does not claim interface status semantics beyond the reserved-zero return.
  - This packet does not claim project-specific interface status handling behavior.
