packet_version: 1
status: reviewed_pilot
created_at: 2026-06-08

target:
  surface: governed_table_entry
  table: ss_hub_interrupt_endpoint_matrix
  entry_id: usb3_ss_hub_ep_bInterval

evidence:
  spec: usb32
  section: "10.15.1"
  page: null
  document_ref:
    primary_source_id: usb32_spec
    fallback_source_id: archive_usb32_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    USB 3.2 Revision 1.1 Section 10.15.1 specifies bInterval value 8 for
    the SuperSpeed hub status-change endpoint and identifies it as the
    maximum allowable interval.
  reviewer_note: >
    Reviewed for field identity only: field name bInterval, required value 8,
    and the maximum-allowable-interval boundary.
    Does not verify actual host scheduling, polling latency, interrupt delivery
    timing, or firmware bInterval assignment correctness.

review_checklist:
  - Confirm §10.15.1 is the correct USB 3.x hub interrupt endpoint surface.
  - Confirm bInterval is 8 and is labeled as the maximum allowable interval.
  - Confirm no scheduling, latency, or firmware behavior is claimed.

verification_scope:
  claim: field_identity_constraint_encoding_only
  excludes:
    - actual host scheduling or polling latency guarantees
    - interrupt delivery timing behavior
    - firmware bInterval assignment correctness
    - USB-IF polling compliance testing

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for entry-level verified promotion for `usb3_ss_hub_ep_bInterval`.
    Promotion limited to field name, required value 8, and boundary identity only.

non_claims:
  - This packet does not verify actual host scheduling or polling latency.
  - This packet does not verify firmware bInterval assignment correctness.
  - This packet does not establish table-level verification.
