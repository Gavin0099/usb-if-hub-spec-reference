packet_version: 1
status: reviewed_pilot
created_at: 2026-06-07

target:
  surface: governed_table_entry
  table: ss_port_status_bit_matrix
  entry_id: ss_wPortChange.bit6.C_PORT_CONFIG_ERROR

evidence:
  spec: usb32
  section: "10.14.2.6"
  page: null
  document_ref:
    primary_source_id: usb32_spec
    fallback_source_id: archive_usb32_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    USB 3.2 Section 10.14.2.6 Table 10-12 defines wPortChange bit 6 as
    C_PORT_CONFIG_ERROR: set when a port configuration error has occurred,
    meaning a SuperSpeed device attached to the port failed to configure
    successfully. This bit is SS-only.
  reviewer_note: >
    Reviewed for bit name and bit position identity only: bit name
    C_PORT_CONFIG_ERROR, field wPortChange, bit position 6. Does not verify
    configuration failure conditions, hub response behavior, or xHCI
    enumeration error handling.

review_checklist:
  - Confirm Section 10.14.2.6 Table 10-12 is the correct SS port change surface.
  - Confirm C_PORT_CONFIG_ERROR is at wPortChange bit 6.
  - Confirm this is SS-only (not present in USB 2.0).
  - Confirm spec_family=usb3 is consistent.
  - Confirm no configuration failure behavior or error handling is claimed.

verification_scope:
  claim: bit_name_and_position_only
  excludes:
    - configuration failure conditions
    - hub response to config error
    - xHCI enumeration error handling
    - full USB 3.x compliance

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for single-entry bit name and position identity promotion for
    `ss_wPortChange.bit6.C_PORT_CONFIG_ERROR`. Promotion limited to bit name
    and bit position identity only.

non_claims:
  - This packet does not verify configuration failure conditions or hub response.
  - This packet does not claim xHCI enumeration error handling.
  - This packet does not establish table-level verification.
  - No USB 3.x electrical or signal compliance is claimed.
