packet_version: 1
status: reviewed_pilot
created_at: 2026-06-07

target:
  surface: governed_table_entry
  table: ss_hub_class_request_matrix
  entry_id: usb3_clear_feature_port

evidence:
  spec: usb32
  section: "10.14.2"
  page: null
  document_ref:
    primary_source_id: usb32_spec
    fallback_source_id: archive_usb32_pdf
    authority_expectation: normative_official_preferred
  quoted_surface: reviewer_paraphrase_only
  quoted_surface_summary: >
    USB 3.2 Section 10.14.2 defines CLEAR_FEATURE (bRequest=0x01) addressed
    to a port recipient (bmRequestType=0x23, wValue=feature_selector,
    wIndex=port_number, wLength=0). SS-specific clear selectors include
    C_PORT_LINK_STATE (0x19) and C_PORT_CONFIG_ERROR (0x1E).
  reviewer_note: >
    Reviewed for request linkage identity only: request name CLEAR_FEATURE,
    bRequest=0x01, port recipient, bmRequestType=0x23. Does not verify
    C_PORT_LINK_STATE or C_PORT_CONFIG_ERROR behavior, or any port state change.

review_checklist:
  - Confirm Section 10.14.2 is the correct SS hub class request surface.
  - Confirm CLEAR_FEATURE is bRequest=0x01 for port recipient.
  - Confirm bmRequestType=0x23 (host-to-device, class, other).
  - Confirm no SS-specific clear selector behavior or port state is claimed.

verification_scope:
  claim: request_linkage_identity_only
  excludes:
    - C_PORT_LINK_STATE clear behavior
    - C_PORT_CONFIG_ERROR clear behavior
    - port state changes from CLEAR_FEATURE
    - SS feature selector value semantics
    - full USB 3.x compliance

result:
  eligible_for_verified: true
  evidence_status: reviewed
  promotion_note: >
    Eligible for single-entry request linkage identity promotion for
    `usb3_clear_feature_port`. Promotion limited to request name, bRequest
    value, recipient, and bmRequestType identity only.

non_claims:
  - This packet does not verify C_PORT_LINK_STATE or C_PORT_CONFIG_ERROR behavior.
  - This packet does not claim port clear feature state semantics.
  - This packet does not establish table-level verification.
