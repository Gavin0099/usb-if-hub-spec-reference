# Session Memory — 2026-06-01 (UTC)

## Session Summary

Phase 5B table fingerprint drift probe implementation, Phase 4 debt clearance,
CI wire-up, and governance framework hardening.

## Work Completed

### Phase 5B — Table Fingerprint Baseline & Drift Check (`cffc9fb`)
- Created `scripts/probe_table_fingerprint.py`
  - `--mode baseline`: SHA-256 fingerprints all manifest-listed governed tables,
    appends entries to `evidence/table_fingerprint_baseline.jsonl`
  - `--mode check`: re-hashes tables, compares to latest baseline entry per
    `table_id`, exits 1 and emits `DRIFT_DETECTED` receipt on any hash change
  - Authority ceiling: `table_content_fingerprint_drift_only`
- Created `scripts/smoke_probe_table_fingerprint_fixtures.py` — 3 cases:
  `no_drift` (PASS), `hash_changed` (DRIFT_DETECTED as expected), `baseline_fresh` (PASS)
- Real-table validation: baseline → check on 3 governed tables, 0 drift, PASS
- Evidence: `evidence/table_fingerprint_baseline.jsonl`,
  `evidence/validation_receipt_table_fingerprint_baseline.json`,
  `evidence/validation_receipt_table_fingerprint_drift.json`

### Phase 4 Debt Clearance (`31e519c`)
- Committed `tables/port_status_bit_matrix.yaml` — 16 entries across
  wHubStatus/wHubChange/wPortStatus/wPortChange, claim_level=inferred
- Committed `validate_port_status_bit_matrix.py` (R1-R6) + smoke 4/4 PASS
- Committed `validate_usb20_hub_class_request_manifest.py` (M1-M8) + smoke 4/4 PASS
- Committed `build_phase4_consumer_access_closeout.py` and Phase 4 closeout doc
- Updated `exports/usb20_hub_class_request_manifest.yaml` with `governed_tables`
  section (required input for probe_table_fingerprint.py)

### Governance Framework (`fe8ae42`)
- AGENTS.md: Review Tasks gate, Commit Checkpoint convention, Canonical Memory
  Writer Rule added
- governance-validation.yml: port_status + manifest smoke steps wired into
  required fixture-smoke CI gate

### CI Wire-up (`58ded45`)
- fixture-smoke gate: added `smoke_probe_table_fingerprint_fixtures.py` step
- advisory job: added port_status_bit_matrix, manifest access contract, and
  table fingerprint drift check (`probe_table_fingerprint --mode check`) steps;
  all outputs uploaded as CI artifacts

## Key Decisions

- `probe_table_fingerprint.py` scoped to local table content fingerprinting only;
  does not interact with remote URL monitor (`usbif_source_monitor.py`)
- Baseline JSONL uses append + last-write-wins semantics; compact mode deferred
- Table fingerprint drift check added to advisory CI (not required gate) since
  baseline.jsonl is a committed artifact — an empty baseline causes NOT_IN_BASELINE
  errors, not a hard block

## Open Items

- C (medium): `probe_table_fingerprint.py --mode baseline` is pure-append; no
  compaction mechanism. Long-running repos will accumulate stale entries.
  Mitigation: periodic re-baseline commit replaces stale entries via last-write-wins.
- E (medium): `tables/port_status_bit_matrix.yaml` still at
  `claim_level: inferred / evidence_status: review_required`.
  Upgrade path: PDF section 11.24.2.7 bit-by-bit verification + section_anchor
  fields per entry.
- F (low): cross-repo traceability links in consuming repo
  (USB-Hub-Firmware-Architecture-Contract TRACEABILITY_MATRIX.md) still absent.
- PLAN.md: not updated to reflect Phase 4 done; update in next session if needed.

## Repo State

- class_request_matrix: 12 entries, 9/9 request families, coverage_status: complete
- feature_selector_matrix: 25 entries, port namespace 0-22: 23/23, complete
- port_status_bit_matrix: 16 entries (4 fields × bits 0-1 hub, 0-15 port), draft
- All entries: claim_level=inferred, evidence_status=review_required
- table_fingerprint_baseline.jsonl: 3 tables, recorded 2026-06-01T06:20:17Z
- CI: fixture-smoke gate covers 14 smoke runners; advisory covers 9 validators
- No PDF section-level verification performed
